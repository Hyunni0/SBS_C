#include <stdio.h>

/*
    # 포인터와 배열의 관계

*/
int main(void){
    int arr[3] = {1,2,3};

    // 인덱스를 통한 배열 접근
    printf("인덱스를 통한 배열 접근 \n");
    printf("%d %d %d \n", arr[0], arr[1], arr[2]);

    // 포인터 연산을 통한 배열 접근
    

}